---
# all applications use these settings and services
memory: 512mb
instances: 1
applications:
- name: foia
  path: .
  stack: cflinuxfs2
  timeout: 180
  services:
    - foia-db
    - foia-public-bucket
  env:
    DISABLE_COLLECTSTATIC: 1
